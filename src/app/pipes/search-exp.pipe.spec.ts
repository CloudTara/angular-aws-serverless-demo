import { SearchExpPipe } from './search-exp.pipe';

describe('SearchExpPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchExpPipe();
    expect(pipe).toBeTruthy();
  });
});
