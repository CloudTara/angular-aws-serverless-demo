import { SearchLocPipe } from './search-loc.pipe';

describe('SearchLocPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLocPipe();
    expect(pipe).toBeTruthy();
  });
});
